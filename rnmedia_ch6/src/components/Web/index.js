import { StyleSheet, Text, View } from 'react-native'
import React from 'react'

const Web = ({data}) => {
  return (
    <View>
      <Text>{data}</Text>
    </View>
  )
}

export default Web

const styles = StyleSheet.create({})